{
  "devDependencies": {
    "typescript": "^4.3.5"
  },
  "scripts": {
    "build": "tsc  --build tsconfig.json",
    "start": "npm run build && node tsbuild/src/main.js"
  },
  "dependencies": {
    "axios": "^0.21.1"
  }
}
