{
  "devDependencies": {
    "typescript": "^4.3.5"
  },
  "scripts": {
    "build": "tsc  --build tsconfig.json",
    "start": "if [ -d './dist' ] \nthen \n rm -R ./dist/* \nfi \nnpm run build && node ./dist/src/main.js"
  },
  "dependencies": {
    "axios": "^0.21.1"
  }
}
